spring:
  application:
    name: RiskNarrative
  jpa:
      generate-ddl: true
      show-sql: true
      open-in-view: false

  jackson:
    property-naming-strategy: SNAKE_CASE

truproxy:
  base:
    url: https://exercise.trunarrative.cloud/